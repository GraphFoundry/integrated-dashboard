# Backend API base URL (PRODUCTION ONLY)
# In development, Vite proxy handles /api -> localhost:7000 automatically
# Set this for production builds to point to the actual backend URL
VITE_API_BASE_URL=http://localhost:7000
